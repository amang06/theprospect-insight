version: 1.0
runtime: nodejs16
build:
  commands:    
    build:
      - npm install 
      - npx playwright install chromium --with-deps
      - npx playwright install-deps                        
run:                              
  command: npm run dev
  network:
    port: 3000