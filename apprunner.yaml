version: 1.0
runtime: nodejs18
build:
  commands:    
    build:
      - npm install 
      - npx playwright install chromium
      - apt install apt
      - npx playwright install-deps                        
run:                              
  command: npm run dev
  network:
    port: 3000