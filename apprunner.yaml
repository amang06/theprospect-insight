version: 1.0
runtime: nodejs18
build:
  commands:    
    build:
      - npm install 
      - npx playwright install chromium
      - npx playwright install-deps                        
run:                              
  command: npm run dev