version: 1.0
runtime: nodejs16
build:
  commands:    
    build:
      - npm install 
      - npx playwright install --with-deps chromium                   
run:                              
  command: npm run dev
  network:
    port: 3000